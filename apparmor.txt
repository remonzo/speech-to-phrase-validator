#include <tunables/global>

profile addon_aa6099a0_stp_validator flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability net_raw,
  capability setgid,
  capability setuid,
  capability sys_admin,
  capability dac_override,

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your addon
  /data/** rw,

  # Start new profile for service
  /app/run.sh cx -> speech_to_phrase_validator,

  profile addon_aa6099a0_stp_validator flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>

    # Receive signals from S6-Overlay
    signal (receive) set=(term, kill) peer=*,

    # Access to options.json and other files within your addon
    /data/** rw,
    /app/** r,
    /app/src/** r,

    # Service specifics
    /app/run.sh r,
    /bin/bash ix,
    /usr/bin/python3* ix,
    /usr/bin/env r,
    /usr/bin/uname ix,
    /etc/passwd r,
    /etc/group r,

    # Speech-to-Phrase shared directories
    /share/speech-to-phrase/** r,

    # Python
    /usr/lib/python3*/** r,
    /usr/local/lib/python3*/** r,

    # SSL certificates
    /etc/ssl/openssl.cnf r,
    /etc/ssl/certs/** r,

    # DNS
    /etc/resolv.conf r,
    /etc/hosts r,

    # Network
    capability net_bind_service,

    # Suppress PaX warnings
    deny /proc/sys/kernel/yama/ptrace_scope w,
  }
}